# User-Authentication-System
Built a REST API for user registration and login with JWT-based authentication and protected routes Hashed passwords with bcrypt and stored users in MongoDB Atlas using Mongoose Built register and login pages with HTML, CSS and Axios, using token-based session checks
